services:
  - type: web
    name: merch-app
    env: node
    buildCommand: |
      npm install
      npm run build
    startCommand: |
      npx vite preview --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
    healthCheckPath: /
